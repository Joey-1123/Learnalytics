version: '3.9'

services:
  web:
    build: .
    container_name: learnalytics_app
    command: gunicorn learnalytics.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
restart: always